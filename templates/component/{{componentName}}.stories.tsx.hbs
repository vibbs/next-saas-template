import { ComponentMeta, ComponentStory } from '@storybook/react';
import { {{capitalize componentName}},  {{capitalize componentName}}Props } from './{{componentName}}';
import { mock{{capitalize componentName}}Props } from './{{componentName}}.mocks';

export default {
    title: 'templates/{{capitalize componentName}}',
    component: {{capitalize componentName}},
    // More on argTypes: https://storybook.js.org/docs/react/api/argtypes
    argTypes: {},
} as ComponentMeta<typeof {{capitalize componentName}}>;

// More on component templates: https://storybook.js.org/docs/react/writing-stories/introduction#using-args
const Template: ComponentStory<typeof {{capitalize componentName}}> = (args) => (
    <{{capitalize componentName}} {...args} />
);

export const Base = Template.bind({});
// More on args: https://storybook.js.org/docs/react/writing-stories/args

Base.args = {
    ...mock{{capitalize componentName}}Props.base,
} as {{capitalize componentName}}Props;
