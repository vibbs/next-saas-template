import { {{capitalize componentName}}Props } from './{{componentName}}';

const base: {{capitalize componentName}}Props = {
    sampleTextProp: 'Hello world!',
};

export const mock{{capitalize componentName}}Props = {
    base,
};
